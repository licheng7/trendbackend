<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.arp.trend.repository.biz.manual.CasAcademicianChinaManualMapper" >
  <resultMap id="BaseResultMap" type="cn.arp.trend.entity.biz.ContrastAcademicianByFieldResult" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="research_field" property="statYear" jdbcType="VARCHAR" />
    <result column="nf" property="pid" jdbcType="VARCHAR" />
    <result column="num" property="cid" jdbcType="BIGINT" />

  </resultMap>

  <select id="contrastByField" resultType="java.util.HashMap">
    SELECT ANY_VALUE(research_field) AS research_field, ANY_VALUE(dxnf) AS nf, COUNT(*) AS num
    FROM (
    SELECT field_id, research_field, b.jgbh, b.jgmc, dxnf
    FROM (
    SELECT dxnf, gzdw_gf1 AS jgmc
    FROM cas_academician_china
    <where>
    qssj IS NULL AND dxnf&lt;=#{2}
    </where>
    ) AS a
    INNER JOIN(
    SELECT *
    FROM relation_field_affiliation
    <where>
    user_id = "#{0}"
    </where>
    AND field_id #{0}
    ) AS b
    ON a.jgmc=b.jgmc
    UNION ALL
    SELECT field_id, research_field, b.jgbh, b.jgmc, dxnf
    FROM (
    SELECT dxnf, gzdw_gf1 AS jgmc
    FROM cas_academician_cae_china
    <where>
    qssj IS NULL AND dxnf&lt;=#{2}
    </where>
    ) AS a
    INNER JOIN(
    SELECT *
    FROM relation_field_affiliation
    <where>
    user_id = "#{0}"
    </where>
    AND field_id #{3}
    ) AS b
    ON a.jgmc=b.jgmc
    ) AS c
    GROUP BY research_field, dxnf;
  </select>
</mapper>